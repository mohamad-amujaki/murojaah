# ============================================
# Murojaah — Environment Variables
# ============================================
# Copy this file to .env and fill in the values.
# For Cloudflare Workers: use `wrangler secret put <NAME>`
# For VPS: source this file or use systemd drop-in

# ——— Application ———
NODE_ENV=production

# ——— Database (MySQL) ———
# Tables are prefixed mu_ — see packages/db/src/schema.ts.
MU_DB_HOST=your-mysql-host
MU_DB_PORT=3306
MU_DB_NAME=your-database-name
MU_DB_USER=your-database-user
MU_DB_PASSWORD=your-database-password

# ——— Redis (optional) ———
# Shared rate-limit store. Docker Compose sets this automatically to the
# bundled redis service; without it, rate limiting falls back to in-memory
# (fine for a single container).
# MU_REDIS_URL=redis://redis:6379

# ——— Google OAuth ———
# Get from: https://console.cloud.google.com/apis/credentials
MU_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
MU_GOOGLE_CLIENT_SECRET=your-google-client-secret

# ——— Email / SMTP (for password reset) ———
# Supports any SMTP provider (Gmail, SendGrid, Mailgun, AWS SES, etc.)
# If not set, password reset will log to console (dev only)
MU_SMTP_HOST=smtp.gmail.com
MU_SMTP_PORT=587
MU_SMTP_USER=your-email@gmail.com
MU_SMTP_PASS=your-app-password
MU_FROM_EMAIL=noreply@murojaah.app
MU_FROM_NAME=Murojaah

# ——— App URL (for password reset links) ———
MU_APP_URL=https://murojaah.yourdomain.com

# ——— Optional: CDN / External Services ———
# EQuran.id is used as fallback if D1 cache is empty
MU_EQURAN_API_BASE=https://equran.id/api/v2
